#!/usr/bin/env bash
# Pre-commit: the full blocking gate battery (check-all) — qmldir consistency,
# fatal-subset qmllint, IPC parity, unit tests, tool availability.
repo=$(git rev-parse --show-toplevel)
"$repo/tools/check-all"
